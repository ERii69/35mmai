/** Account menu link to the project dashboard (`/pro/app`). */
export const PRO_STUDIO_NAV_LABEL = "Studio";

/** Primary CTA when the user already has Pro access. */
export const PRO_OPEN_STUDIO_LABEL = "Open studio";

/** Marketing /pro CTA when entitled — dashboard with workflows row. */
export const PRO_CONTINUE_STUDIO_LABEL = "Continue in studio";
